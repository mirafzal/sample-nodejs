import React from 'react';

function App() {
    return (
        <div style={{ textAlign: 'center', marginTop: '50px' }}>
            <h1>React + Nginx Docker Multi-Stage Build</h1>
            <p>This is a production build served by Nginx!</p>
        </div>
    );
}

export default App;
